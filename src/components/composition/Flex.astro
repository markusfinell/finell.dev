---
const propClasses = ["column", "justify-between", "justify-center"];
const { tagName = "div", ...props } = Astro.props;
const Tag = tagName;
---

<Tag class:list={["flex", ...propClasses.map((className) => Astro.props[className] && className)]} {...props}>
  <slot />
</Tag>

<style>
  .flex {
    display: flex;
  }

  .flex.column {
    flex-direction: column;
  }

  .flex.justify-between {
    justify-content: space-between;
  }

  .flex.justify-center {
    justify-content: center;
  }
</style>
