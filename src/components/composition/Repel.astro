---
const { tagName = "div", ...props } = Astro.props;
const Tag = tagName;
---

<Tag class:list={["repel"]} {...props}><slot /></Tag>

<style>
  .repel:not([class*="space--"]) {
    --space: var(--space--md, 1rem);
  }

  .repel {
    display: flex;
    justify-content: space-between;
    gap: var(--space);
  }
</style>
