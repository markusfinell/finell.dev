---
const { tagName = "div", className = "", gap = "", ...props } = Astro.props;
const Tag = tagName;
---

<Tag class:list={["stack", gap && `gap--${gap}`, className]} {...props}><slot /></Tag>

<style>
  .stack {
    display: flex;
    flex-direction: column;
  }
</style>
