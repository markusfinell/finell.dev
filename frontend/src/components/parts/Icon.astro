---
export interface Props {
  icon: string;
}

const { icon } = Astro.props as Props;
const { default: innerHTML } = await import(/* @vite-ignore */ `/icons/${icon}.svg?raw`);
---

<Fragment set:html={innerHTML} />

<style is:global>
  .icon svg {
    width: 1em;
    height: 1em;
  }
</style>
